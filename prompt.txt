Please implement Gemini's Automated Smart Nutrition Lookup Engine across the frontend and backend. 1. BACKEND ROUTE & LOOKUP DICTIONARY: In server.js, build a new POST /api/estimate-nutrition endpoint route. It must accept a foodName string in the body. Inside, build a high-accuracy reference dictionary of staple foods containing accurate metrics per 100g (e.g., Chicken Breast: 165 cal, 31g P, 0g C, 3.6g F; Cooked White Rice: 130 cal, 2.7g P, 28g C, 0.3g F; Oatmeal: 389 cal, 16.9g P, 66g C, 6.9g F; Whole Egg: 155 cal, 13g P, 1.1g C, 11g F; Banana: 89 cal, 1.1g P, 23g C, 0.3g F). For non-staple strings, calculate a safe generic calorie-to-macro allocation fallback matching the 4/4/9 calorie rule so it always balances. Return a uniform JSON payload. 2. FRONTEND BLUR LOGIC: In public/app.js, add a 'blur' or debounced input event listener to the Food Name input fields inside the custom logging forms across all 4 meal categories (Breakfast, Lunch, Dinner, Snacks). When a user finishes typing a name, fire an asynchronous fetch request to /api/estimate-nutrition. 3. AUTO-POPULATE & GRAM SCALE: When the payload returns, automatically populate the kcal, protein, carbs, and fat input boxes on screen with the 100g database metrics. Instantly re-run the layout's calculation script so that if a weight (e.g., 150g) is already filled in, the absolute meal log math adjusts and updates the totals preview on screen dynamically. 4. SYSTEM STABILITY: Do NOT touch or break your system default appearance query preferences, permanent bottom navigation bar fixed anchors, local storage login persistence token scripts, camera barcode viewfinders, or macro pie charts.
