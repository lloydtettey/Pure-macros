Please surgically fix the double-text stacking layout bug and apply premium iOS native CSS touch-ups. 1. REMOVE DOUBLE-TEXT BLEEDING: In styles.css, ensure that all absolute full-screen sub-view containers (such as the 13 More tab subpages, Settings views, and Quick Add sheets) are assigned a high stacking order position (z-index: 100 or higher) and a completely solid, solid opaque light background-color property (e.g., background: #ffffff !important;). In app.js, whenever a sub-view is opened, explicitly toggle a class or update style visibility properties on the underlying parent view container to add display: none or hidden visibility, completely preventing background elements or text from stacking or bleeding through. When closing a sub-view via the back arrow or edge-swipe, restore the parent tab view's visibility instantly. 2. STRIP TAP HIGHLIGHTS: Add a global CSS reset rule at the very top of styles.css to erase the browser-native gray box flash when tapping buttons or rows: * { -webkit-tap-highlight-color: transparent !important; -webkit-touch-callout: none !important; user-select: none; }. 3. FIX TAB BAR SAFE AREA padding: Locate the bottom navigation bar container class (e.g., .bottom-nav or .tab-bar) inside styles.css and update its styling properties to cleanly account for the iPhone home indicator line bar space: padding-bottom: calc(12px + env(safe-area-inset-bottom)) !important; height: calc(64px + env(safe-area-inset-bottom)) !important;. 4. SYSTEM STABILITY: Keep your working remember-me password persistence, edge swipe-back gesture tracking loops, automatic macro estimation engines, and macro pie charts fully active and untouched.
