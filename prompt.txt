Please implement Gemini's Month/Year Scroll-Selector Overlay inside the calendar dropdown component. 1. HEADER SELECTION TOGGLE: Wrap the Month/Year text string at the top of the calendar dropdown container inside a clickable button element (#calendar-month-year-btn) and append a small downward chevron arrow (?) next to it. 2. PICKER OVERLAY UI DESIGN: Inside the dark-glass #calendar-dropdown box, create a sub-overlay element named #month-year-picker-view (defaulting to hidden). Clicking the Month/Year button must smoothly toggle this view to show, while completely hiding the day headers row and day digits grid. Build this view with two side-by-side vertical scrollable columns: Left column for Months (January - December) and Right column for Years (e.g., 2022 to 2030). Style it with premium dark aesthetics: dimmed text for inactive parameters, a centered horizontal transparent selection accent highlight bar, and crisp white bold text for the focused selection. 3. EVENT LOGIC HANDLING: Do NOT modify or alter the existing working left/right arrow click triggers. When a user clicks or scrolls to a specific month or year inside this new scroll menu, instantly update the internal calendar month/year reference state variables. Automatically hide the picker view, re-display the day headers and calendar grid, and refresh the day matrix cells to display the newly selected month's dates perfectly. 4. SYSTEM STABILITY: Keep your 6 boolean push notifications configuration checkboxes, customizable 3-profile macro target selectors, live webcam camera viewfinder scripts, speech voice recorders, 13 settings panels, and neon macro pie charts completely operational and untouched.
