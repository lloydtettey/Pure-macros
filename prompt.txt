Please implement Gemini's Registration Sync, Live Input Modals, Privacy/Help/Sync Sub-views, Startup Flicker Eradication, and Performance Tuning. 1. REGISTRATION DATA & LIVE EDIT MODALS: In public/app.js, dynamically map registration fields to compute a Date of Birth string (e.g. converting Age 22 into '01 Jan 2004' based on current year 2026) and format strings into imperial ('5 ft, 10 in') or metric arrays based on userUnitPreference, saving variables inside a unified user_profile localStorage object. Tapping any row in the Personal Details view must launch an elegant modal input wrapper (date pickers for DOB, text fields for location/email). Saving overrides the user_profile object, refreshes displayed labels instantly, and recalibrates linked components like weight logs and graphs automatically. 2. PRIVACY, HELP, & ANIMATED SYNC VIEWS: Build three fullscreen overlay screens named #privacy-subview, #help-subview, and #sync-subview: - A) PRIVACY: Append toggle switches for advertising preferences, diagnostics, and profile visibility, alongside data request and account deletion pop-up confirmation alerts. - B) HELP: Include a functional interactive FAQ Search Bar, accordion-style dropdown info rows for common questions, and a structured Contact Support form template (Subject, Message textareas). - C) SYNC: Construct a central 'Sync Database' action button displaying a looping arrow icon. Clicking it triggers a rotating CSS keyframe animation wheel over a 1.5-second cloud sync progress loader, updating text readouts to a green '? Last Synced: Just Now'. 3. ERADICATE STARTUP FLICKER BUG: In styles.css, ensure the very first CSS rule explicitly sets both html and body to a solid deep dark background (#0f0f12) with white typography to block browser raw-render white flash stutters. Set the splash screen wrapper style to postion: fixed, 100vw/100vh, background: #0f0f12, and z-index: 9999. Use CSS keyframe animations to smoothly fade in the pulsing emblem, destroying the overlay cleanly over a 0.3s opacity curve only when layouts, DOM nodes, and localStorage states are fully parsed. 4. GPU TRANSITION PERFORMANCE TUNING: Strip out heavy backdrop-filter blur parameters and complex box-shadow rules on scrollable list item containers, replacing them with solid, opaque backgrounds. Apply will-change: transform; and transform: translate3d(0, 0, 0); to all tabs and sub-views to lock scrolling to a fluid 60fps frame rate. Register all touch gestures (touchstart, touchmove, touchend) with { passive: true } handlers in JS, and wrap dynamic SVG/Canvas drawing algorithms inside requestAnimationFrame loops to prevent scroll-blocking calculation bloat. 5. SYSTEM STABILITY: Keep your exercise step calorie calculators, Garmin/Strava live app connection arrays, locked bottom nav cushy bars, and diary swipe gestures fully functional.
