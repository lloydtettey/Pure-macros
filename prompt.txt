Please execute a comprehensive A-Z QA structural audit and force-fix these integration layers across my production application files using completely generic, anonymous data structures. 1. HIGH-CONTRAST CSS VARIABLE SYSTEM: At the absolute top of styles.css, remove any hardcoded color strings from elements. Implement a crisp color variable schema on the :root node supporting data-theme='light' (main: #f5f5f7, card: #ffffff, text-primary: #111111, text-secondary: #555555, alert-bg: #ffeeba, alert-text: #856404, border: #e5e5ea) and data-theme='dark' (main: #0f0f12, card: #1c1c1e, text-primary: #ffffff, text-secondary: #a0a0a5, alert-bg: #2c1a04, alert-text: #ffcc00, border: #2c2c2e). Apply background-color: var(--alert-bg); color: var(--alert-text); to the Push Notifications warning/alert banner to ensure total readability. Set all notification checklist option labels to color: var(--text-primary); with a bold font-weight so they stand out crisply in both modes. 2. ZERO-LAG THEME SWITCHING: In public/app.js, completely replace any complex theme-changing element loops or page-reloading scripts with a direct, high-performance attribute hook: document.documentElement.setAttribute('data-theme', selectedTheme);. For the System Match preference option, bind a window.matchMedia('(prefers-color-scheme: dark)') event tracking listener node to prevent system crashes and fluidly update the data-theme attribute on the fly when the system changes profiles. 3. CRASH-PROOF STARTUP & AUTH INITIALIZATION: Before reading or rendering any viewport components on boot, check if core variables exist in localStorage. If null or undefined, instantly seed a safe, empty default object via localStorage.setItem('app_user_profile', JSON.stringify({ height: 180, weight: 80, age: 25, gender: 'unspecified', setupComplete: false })). Enforce that login/registration screen panels utilize high-performance display class toggling (.hidden adding or removing mechanisms) instead of heavy full-browser repaints. 4. REACTIVE CORE DASHBOARD LOOP: Write a unified, central updateMainDashboard() renderer function in public/app.js. This module must programmatically read logged food totals (calories, protein, carbs, fat) from today's active file database, pull target variables from localStorage, mathematically compute progress margins, and instantly repaint the visual calorie ring stroke-dasharray properties and numeric values via requestAnimationFrame. Fire updateMainDashboard() immediately whenever an entry is added, edited, or swiped-to-deleted to guarantee instantaneous, lag-free real-time synchronization with zero manual page refreshes. 5. SYSTEM STABILITY: Keep your 26-category recipe variants engines, Weekly Digest charts, Base64 profile avatar file loaders, dynamic oz/ml hydration cups, and native swipe-to-delete touch gesture listening scripts completely operational and untouched.
