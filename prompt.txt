Please implement Gemini's Debounced Theme-Switching Timer and Race Condition Protection across my app files. 1. OUTER MODULE MODULE TIMEOUT SCOPE: In public/app.js at the top of the file or outer module scope, declare a persistent variable handle: let themeTimeout = null;. 2. DEBOUNCE TIMEOUT CLEAR MATRIX: Inside the central setTheme() or applyTheme() function where the 'no-transitions' class is managed on document.body, inject an immediate validation check: if (themeTimeout) { clearTimeout(themeTimeout); }. 3. RE-LOCK AND CHRONOLOGICAL REASSIGNMENT: Add 'no-transitions' to document.body, apply the theme attribute change instantly, and then assign the new 250ms setTimeout loop directly back to our active tracking variable: themeTimeout = setTimeout(() => { document.body.classList.remove('no-transitions'); themeTimeout = null; }, 250);. This guarantees that your protective animation block stays securely clamped shut until exactly 250ms AFTER the user has completely finished tapping the rows. 4. SYSTEM STABILITY: Keep your 25-second relaxed boot thresholds, box-shadow and filter transition bans, updateMainDashboard() reactive progress loops, 26-category recipe variants matrices, native swipe-to-delete touch gesture scripts, and Privacy/Help/Sync sub-views fully operational and untouched.
