Please implement Gemini's iOS Safari WebKit Anti-Crash and Theme Loop Protection specification across my codebase. 1. ERADICATE INFINITE LOOP RELOADS: In public/app.js, completely audit the theme application logic. Ensure that applying 'data-theme' to the HTML tag does NOT invoke window.location.reload() or trigger cascading state mutations that fire repeatedly. Wrap the boot sequence theme scanner in a strict try/catch block so that any corrupt or missing localStorage values instantly fall back to a safe default 'dark' theme setting without crashing the app load pipeline. 2. SECURE SYSTEM MEDIA SCHEME LISTENER: Refactor the window.matchMedia('(prefers-color-scheme: dark)') event tracker so it ONLY fires when the physical iPhone settings profile shifts, and explicitly blocks execution if the user has locked in a manual 'light' or 'dark' override setting in localStorage. 3. FIX GPU MEMORY LEAK TRANSITIONS: In styles.css, completely find and remove any wildcard text transitions like * { transition: all ... } or transitions applied to heavy structural grid containers during view toggles. Restrict background transition animations strictly to specific, non-structural color properties: body, .card, .dashboard-container { transition: background-color 0.25s ease, color 0.25s ease; }. This prevents iOS Safari WebKit from overloading memory allocation pools when elements shift classes. 4. NOTIFICATION TEXT CONTRAST INHERITANCE: Double-check the push notifications warning banner block and checkbox choices text nodes to ensure they dynamically read from high-contrast variables (var(--text-primary)) that invert instantly without manual JS calculations. 5. SYSTEM STABILITY: Keep your updateMainDashboard() reactive rendering loops, 26-category recipe variants matrices, and native swipe-to-delete touch gesture scripts fully operational.
