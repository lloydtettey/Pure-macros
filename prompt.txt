Please completely overhaul my application's stylesheet and view management layer to use a solid-color architecture instead of transparent glass layers to stop lag and text bleeding. 1. SOLID OPAQUE DESIGN OVERHAUL: In styles.css, completely strip out all 'backdrop-filter: blur' and 'background: rgba(...)' transparent rules from all 13 subpages, settings boxes, overlays, and main tabs. Implement a premium, high-contrast traditional flat light theme: App viewport background must be a solid, opaque light gray (#f8f9fa !important), and all individual menu rows, content blocks, and form containers must use solid, flat opaque white backgrounds (#ffffff !important). 2. ABSOLUTE VIEW HIDDEN ENFORCEMENT: In app.js, modify the screen toggle functions. Whenever any subpage or '+' log overlay is active, explicitly inject display: none to the underlying dashboard tabs container so background elements are physically removed from rendering, completely ending double-text bleeding and page clutter. 3. CRASH & LATENCY PREVENTION: Erase heavy animations causing layout lagging or crashes when toggling modes or resuming from background processing. 4. SECURE RE-LOGIN PASS: Double-check the login parsing engine in server.js to ensure passwords are consistently authenticated accurately against db.json user nodes on first submission to prevent invalid password errors. 5. SYSTEM STABILITY: Keep your working iPhone swipe-back gestures, custom 3-profile calorie goals, macro pie chart graphics, and open food facts v2 API active.
