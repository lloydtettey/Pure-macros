Please implement Gemini's Dynamic Push Notifications & iOS-Style Scroll-Wheel Picker feature across my app files. 1. PUSH NOTIFICATIONS INTERACTION OVERHAUL: In public/index.html, modify the Push Notifications sub-view header banner text to match: 'Notifications are currently disabled for Pure Macros. To receive notifications, go to Settings.' Clicking the 'Settings' link text row must open a simulated system pop-up pane container (#mock-device-settings) showing an iOS-style 'Allow Notifications' green toggle switch. Switching it ON saves localStorage.setItem('push_notifications_enabled', 'true'), dismisses the pop-up, and automatically fades out the banner on the main page. Wire all 6 checklist items to update localStorage toggle states, and expand the 'Do not notify me between' row to append an interactive start/end time picker when active. 2. iOS-STYLE DRUM WHEEL-PICKER SHEET: Build a full-viewport bottom-sheet container layer named #ios-wheel-picker-sheet (defaulting to hidden) that slides up from the base with a dark glass dimming mask backdrop overlay. The picker's top sticky navigation header tracks a clear exit '?' on the left, centered dynamic topic title, and an action checkmark button (?) on the right. 3. 3D SNAP DRUM CYLINDER WHEEL: In styles.css, engineer the wheel selection track box to feature a high-density vertical scroll container using scroll-snap-type: y mandatory and overflow-y: scroll. Overlay top and bottom linear gradients over the scroll area to fade out inactive choices, highlighting the active string in the absolute center using strict top and bottom border borders and scroll-snap-align: center. 4. MATRIX OPTIONS ARRAY GENERATOR & DATA BINDING: In public/app.js, connect the Goals screen row links for 'Workouts/Week' and 'Minutes/Workout' to toggle this picker sheet open. When opening Workouts/Week, dynamically populate the drum option array text indices from 0 to 28. When opening Minutes/Workout, dynamically populate from 0 to 360 in 1-minute increments. When clicking the save checkmark (?), commit the focused value string straight to localStorage and instantly re-render the displayed number metrics across your subpage lists and relevant fitness dashboard widgets via requestAnimationFrame without needing a manual refresh. 5. SYSTEM STABILITY: Keep your multi-level slide-in privacy dashboards, password form validators, inline pulsing lightning bolt head splash screens, autoplaying looping workout video modals, and remember-me auto logins completely operational.
