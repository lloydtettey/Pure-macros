Please implement Gemini's System Default Theme Option & Real-Time Media Listener architecture across the frontend and backend. 1. APPEARANCE SETTINGS UI UPGRADE: In public/index.html, locate the App Appearance settings sub-view screen container. Inject a third choice card row element labeled 'System Default' right alongside the existing 'Light Mode' and 'Dark Mode' options. Give it a distinct selector toggle handle. 2. THREE-STATE JAVASCRIPT THEME CONTROL: In public/app.js, refactor the theme initialization and toggle script logic to fully support three distinct active states: 'light', 'dark', and 'system'. When 'System Default' is selected, save localStorage.setItem('themePreference', 'system'). Build a helper validation pass that checks the native device media configuration string: window.matchMedia('(prefers-color-scheme: dark)').matches. Instantly apply our solid dark-theme surface classes if true, otherwise fallback to our crisp solid light-theme styling layout. Dynamically refresh the HTML meta theme-color tag to match. 3. REAL-TIME SYSTEM MEDIA QUERY LISTENER: Append a native system event tracking listener node to the media query: window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', e => { if (localStorage.getItem('themePreference') === 'system') { toggle ThemeClasses(e.matches); } }). This ensures that if the physical iPhone toggles color profiles automatically (such as at sunset), the running application frames shift themes immediately with zero lag or manual input. 4. ACTIVE ROW SELECTION HIGHLIGHT: Update the CSS toggle classes so that clicking 'System Default' adds the crisp solid active dark block indicator element to it and strips it from the other two rows seamlessly. 5. SYSTEM STABILITY: Do NOT touch or break your working persistent login memory, hardware camera viewfinders, voice log transcriptions, dark mode Plan tab wizard panels, or locked bottom nav cushion bar positions.
