Please implement Gemini's iOS Safari WebKit Crash Armor and Theme Toggle Rate-Limiter specification across my app files. 1. 50MS TRANSITION BYPASS FOR SWAPS: In styles.css at the very bottom, append the rule: .no-transitions * { transition: none !important; }. In public/app.js, inside the setTheme() or theme-changing logic, explicitly add the class 'no-transitions' to document.body BEFORE mutating the HTML data-theme attribute. After updating the theme attribute row, invoke a 50ms setTimeout loop wrapper to strip 'no-transitions' away from document.body. This cuts layout calculation strains from overwhelming the mobile iOS GPU. 2. CLEAN STRIP ALL 'TRANSITION: ALL' STRINGS: Search styles.css for ALL occurrences of transition: all or wildcard transition properties. Overwrite them completely using targeted, non-structural style property animations: transition: background-color 0.25s ease, color 0.25s ease, border-color 0.25s ease, opacity 0.25s ease;. Explicitly block padding, margin, width, or height dimension rules from transitioning globally to avoid layout calculation cascades. 3. 350MS TOGGLE RATE-LIMIT DEBOUNCER: Modify the appearance switch or theme toggle click listeners. Build a local control boolean lock (isTransitioning = true) that blocks rapid-fire tapping inputs. When tapped, fire the transition, hold the locked state, and release it back to false after a 350ms window. 4. 4-SECOND ASYNC NETWORK TIMEOUT: Ensure your app boot script initializes layout skeletons instantly. Wrap all initial fetch queries to Render server backend endpoints inside an async/await try/catch structure. Wire an explicit Promise.race() 4-second timeout gate threshold: if a server network request takes longer than 4 seconds, break the pending state loop safely without freezing the interface, and gracefully reveal the login/register authorization dashboard screen. 5. SYSTEM STABILITY: Keep your updateMainDashboard() reactive rendering loops, 26-category recipe variants matrices, native swipe-to-delete touch gesture scripts, and Privacy/Help/Sync sub-views fully operational.
