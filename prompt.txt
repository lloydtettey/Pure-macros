Please implement Gemini's Free Tier Cold-Start Shield and Boot Logic Protection across my app files. 1. RELAX BOOT TIMEOUT LIMITS: In public/app.js, locate the bootstrapAuth() / initApp() network Promise.race() timeout threshold gate. Relax the timeout execution limit from 4 seconds to exactly 25 seconds to gracefully accommodate Render's free tier container container sleep spin-up cycles. 2. PROTECT SESSION TOKENS FROM NETWORK ERRORS: Refactor the authentication error mapping sequence. If the initial boot network query times out or returns a generic connection fetch failure, explicitly block the app from clearing the localStorage active token session, and do NOT force the user back onto the register/login screen layout panels. Only clear the user's logged-in token session and redirect to the Auth Gate landing splash if the cloud server explicitly returns a valid HTTP 401 Unauthorized or 403 Forbidden server status response code. 3. IMMERSIVE WAKING UP UI SPLASH CARD: Update the app initialization loader script. If the authentication fetch handshake takes longer than 3 seconds, inject a beautifully styled, subtle 'Waking up server, please wait...' typography indicator or rotating loader spinner widget directly onto the pulsing #app-splash screen so the user knows the application is actively connecting and has not frozen. 4. SYSTEM STABILITY: Keep your 50ms theme switch transition bypass locks, 350ms click debouncers, updateMainDashboard() reactive loops, 26-category recipe variants matrices, native swipe-to-delete touch gesture scripts, and Privacy/Help/Sync sub-views fully operational and untouched.
