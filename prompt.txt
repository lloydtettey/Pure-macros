Please implement these 4 system optimization and user experience bug fixes across my codebase. 1. STOP UNWANTED MOBILE ZOOM: In public/index.html, update the meta viewport tag to include 'user-scalable=no, initial-scale=1, maximum-scale=1'. In styles.css, ensure all text inputs, textareas, and dropdown select fields enforce a minimum font-size: 16px to completely prevent iOS Safari from auto-zooming when fields are focused. 2. COMPACT VOICE LOG PIPELINE: In public/js/app.js (or script.js), update the Voice Log SpeechRecognition success handler. When transcription finishes, instantly inject the resulting text string directly into the main '+' menu's universal food search bar field, trigger the asynchronous Open Food Facts API query lookup loop automatically, and cleanly collapse the mic overlay sheet so the user is immediately looking at their real search rows. 3. ERASE LAG WITH GRAPHICS SPEED: In styles.css, apply 'will-change: transform, opacity;' and 'transform: translate3d(0,0,0);' to all absolute modals, the floating '+' wrapper, the Tinder-style swipe cards deck, and the 13 scrollable settings pages to trigger phone hardware GPU acceleration and delete scrolling latency. 4. LINK SIGN-UP DATA TO WIZARD: Modify the 9-step Meal Planner Onboarding Wizard state logic. When the wizard initializes, check if user physical metric variables (Name, Weight, Goal Weight, Activity Level) already exist in our db.json profile or active onboarding state. If they exist, automatically auto-fill those fields and seamlessly skip Step 2 entirely, advancing the user straight from Step 1 to Step 3 so they never see redundant questions. 5. SYSTEM STABILITY: Keep your functional macro Pie Chart graphics, 3-profile customizable target system settings arrays, hidden Apple sign-in blocks, and responsive layout edge guardrails fully operational and untouched.
