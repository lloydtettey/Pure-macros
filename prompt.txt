Please implement these visual overlap, swipe gesture, and login memory fixes. 1. OPAQUE GLASS BACKING: In styles.css, update all absolute full-screen sub-view containers (the 13 More tab subpages, Settings screens, and Quick Add sheets). Change their background properties to an opaque frosted styling (e.g., a solid light background color or heavily opaque white backdrop) to completely block background text from bleeding through and causing double-text overlaps when pages stack. 2. NATIVE IPHONE SWIPE BACK GESTURE: In public/js/app.js (or script.js), attach touch gesture event listeners (touchstart, touchmove, touchend) to all 13 sub-navigation views. If a user swipes their finger from the left edge of the screen to the right, fluidly animate that sub-view closed, mimicking native iOS navigation. Do NOT remove or modify the existing working manual back arrow buttons. 3. REMEMBER ME LOGIN PERSISTENCE: Fix the authentication state engine. When a user successfully registers or logs into their account, securely cache their session profile token inside localStorage. On application startup, automatically read this token, validate the session against the backend, and bypass the login/register splash screens entirely to drop the user straight onto their active Today dashboard so they never get logged out accidentally. 4. ERASE LATENCY: Maximize rendering speeds by stripping out heavy graphic filter calculations during active touch drag animations to completely lock down scrolling response times. 5. SYSTEM STABILITY: Keep your automatic macro estimating engine, dynamic day-profile calorie selectors, live webcam barcode viewfinder scanners, voice logging transcribers, and macro pie chart canvas graphics fully functional.
